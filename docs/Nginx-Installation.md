# Nginx Installation

## Objective

Install and configure the Nginx web server.

## Installation

```bash
sudo apt update
sudo apt install nginx -y
```

## Verification

```bash
sudo systemctl status nginx
```

The service started successfully and was enabled to start automatically at boot.

Insert Nginx status screenshot here.
